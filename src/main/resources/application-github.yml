spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: username
    password: password
    url: jdbc:mysql://xxx.xxx.xxx.xx/database?characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    type: com.alibaba.druid.pool.DruidDataSource
  jackson:
    default-property-inclusion: non_null

wechat:
  mpAppId:
  mpAppSecret:
  mchId:
  mchKey:
  keyPath:
  notifyUrl:

wx:
  pay:
    appId:
    mchId:
    mchKey:
    keyPath:
    notifyUrl:

server:
  servlet:
    context-path: /sell